var configsArray = [];
var config;

config = {};
config.enableStatus = true; 
config.gamesAndStatus = [
	"Eviona Peek | AFK", 
	730, 570]; 
config.replyMessage = 'Şu anda hourboost açık ben yokum. Sonra tekrar yazarsın canım (Otomatik mesaj).'; 
config.receiveMessages = false; 
config.saveMessages = true; 
configsArray.push(config);

module.exports = configsArray;
